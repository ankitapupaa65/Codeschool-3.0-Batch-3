import { FiterEmployeePipePipe } from './fiter-employee-pipe.pipe';

describe('FiterEmployeePipePipe', () => {
  it('create an instance', () => {
    const pipe = new FiterEmployeePipePipe();
    expect(pipe).toBeTruthy();
  });
});
